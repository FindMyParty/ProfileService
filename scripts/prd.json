{
  "project": "ProfileService",
  "branchName": "ralph/database-migrations",
  "description": "Database Migrations — Pictures, Themes, Characters, Classes e Systems",
  "userStories": [
    {
      "id": "US-001",
      "title": "Criar migration 002_extend_profile_tables.ts com estrutura base",
      "description": "As a developer, I need to create the migration file with correct imports, up() and down() function signatures, and the pictures table so the file is ready for extension.",
      "acceptanceCriteria": [
        "Arquivo criado em src/adapters/outbound/db/migrations/002_extend_profile_tables.ts",
        "Importa `sql` e `Kysely` de 'kysely' exatamente como em 001_create_profiles_table.ts",
        "Exporta `async function up(db: Kysely<unknown>): Promise<void>`",
        "Exporta `async function down(db: Kysely<unknown>): Promise<void>`",
        "Tabela `pictures` criada em up(): id_profile UUID NOT NULL (FK → profiles.id ON DELETE CASCADE), pic VARCHAR(2048) NOT NULL, is_primary BOOLEAN NOT NULL DEFAULT false, PRIMARY KEY (id_profile, pic)",
        "Typecheck passes"
      ],
      "priority": 1,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-002",
      "title": "Adicionar tabelas themes e profile_themes na migration",
      "description": "As a developer, I need the themes lookup table and its join table with profiles added to the migration file.",
      "acceptanceCriteria": [
        "Tabela `themes` criada em up(): id UUID PRIMARY KEY, name VARCHAR(255) NOT NULL UNIQUE",
        "Tabela `profile_themes` criada em up(): id_profile UUID NOT NULL (FK → profiles.id ON DELETE CASCADE), id_themes UUID NOT NULL (FK → themes.id ON DELETE CASCADE), PRIMARY KEY (id_profile, id_themes)",
        "`themes` é criada antes de `profile_themes` em up()",
        "`profile_themes` é dropada antes de `themes` em down()",
        "Typecheck passes"
      ],
      "priority": 2,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-003",
      "title": "Adicionar tabela characters na migration",
      "description": "As a developer, I need the characters table added to the migration file so profiles can have RPG characters.",
      "acceptanceCriteria": [
        "Tabela `characters` criada em up(): id UUID PRIMARY KEY, id_profile UUID NOT NULL (FK → profiles.id ON DELETE CASCADE), name VARCHAR(255) NOT NULL, background VARCHAR(500), level SMALLINT NOT NULL DEFAULT 1, is_alive BOOLEAN NOT NULL DEFAULT true",
        "`characters` é dropada antes de `profile_themes` em down()",
        "Typecheck passes"
      ],
      "priority": 3,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-004",
      "title": "Adicionar tabelas classes, characters_classes e profile_classes na migration",
      "description": "As a developer, I need the classes lookup table and its join tables for characters and profiles added to the migration file.",
      "acceptanceCriteria": [
        "Tabela `classes` criada em up(): id UUID PRIMARY KEY, name VARCHAR(255) NOT NULL UNIQUE",
        "Tabela `characters_classes` criada em up(): id_characters UUID NOT NULL (FK → characters.id ON DELETE CASCADE), id_classes UUID NOT NULL (FK → classes.id ON DELETE CASCADE), PRIMARY KEY (id_characters, id_classes)",
        "Tabela `profile_classes` criada em up(): id_profile UUID NOT NULL (FK → profiles.id ON DELETE CASCADE), id_classes UUID NOT NULL (FK → classes.id ON DELETE CASCADE), PRIMARY KEY (id_profile, id_classes)",
        "`classes` é criada antes de `characters_classes` e `profile_classes` em up()",
        "`characters_classes` e `profile_classes` são dropadas antes de `classes` em down()",
        "Typecheck passes"
      ],
      "priority": 4,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-005",
      "title": "Adicionar tabelas systems, characters_systems e profile_systems na migration",
      "description": "As a developer, I need the systems lookup table and its join tables for characters and profiles added to the migration file, completing the full migration with a correct down() order.",
      "acceptanceCriteria": [
        "Tabela `systems` criada em up(): id UUID PRIMARY KEY, name VARCHAR(255) NOT NULL UNIQUE",
        "Tabela `characters_systems` criada em up(): id_characters UUID NOT NULL (FK → characters.id ON DELETE CASCADE), id_systems UUID NOT NULL (FK → systems.id ON DELETE CASCADE), PRIMARY KEY (id_characters, id_systems)",
        "Tabela `profile_systems` criada em up(): id_profile UUID NOT NULL (FK → profiles.id ON DELETE CASCADE), id_systems UUID NOT NULL (FK → systems.id ON DELETE CASCADE), PRIMARY KEY (id_profile, id_systems)",
        "`systems` é criada antes de `characters_systems` e `profile_systems` em up()",
        "down() dropa todas as 11 tabelas na ordem inversa da criação, sem erros de FK",
        "Ordem de drop em down(): profile_systems, characters_systems, systems, profile_classes, characters_classes, classes, characters, profile_themes, themes, pictures",
        "Typecheck passes"
      ],
      "priority": 5,
      "passes": true,
      "notes": ""
    }
  ]
}
